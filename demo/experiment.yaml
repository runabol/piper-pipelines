name: Hello Experiment
    
tasks:          
  - type: each
    list: "${range(1,2)}"
    itemVar: item1
    iteratee:
      type: each
      list: "${range(1,2)}"
      itemVar: item2
      iteratee:
          type: print
          text: ${item1}-${item2}
    
