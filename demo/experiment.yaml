name: Hello Experiment
    
tasks:          
  - type: each
    list: "${range(1,5)}"
    itemVar: item1
    iteratee:
      type: each
      list: "${range(1,5)}"
      itemVar: item2
      iteratee:
          type: sleep
          millis: 1000 
    
