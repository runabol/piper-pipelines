name: Hello Experiment
    
tasks:          
  - type: each
    list: "${range(1,2)}"
    itemVar: item1
    iteratee:
      type: each
      list: "${range(1,2)}"
      itemVar: item2
      iteratee:
        type: each
        list: "${range(1,2)}"
        itemVar: item3
        iteratee:   
          type: print
          text: ${item2}-${item1}-${item3}
    
