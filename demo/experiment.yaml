name: Hello Experiment
    
tasks:          
  - type: each
    list: "${range(1,100)}"
    iteratee:
      type: each
      list: "${range,10}"
      iteratee: 
        type: print
        text: ${item}
    
