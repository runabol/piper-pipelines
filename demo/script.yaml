name: Hello Script
    
inputs:
  - name: path
    label: Path to list
    type: string
    required: true
    
tasks:      
  - label: List files in ${path}
    name: myoutput
    type: bash
    script: |
          for f in ${path}
          do
            echo "$f"
          done
          
    
